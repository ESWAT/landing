
## Landing Page v3

### Installation

> `brew install node`           <br>
  `gem install compass`         <br>
  `npm install grunt-cli -g`    <br>
  `npm install -d`

### Running

> `grunt run`                   <br>
  `open http://localhost:9000`
