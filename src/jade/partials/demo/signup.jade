
article.view.view-demo-signup

    section.signup-form-container

        .background-images
            img.image-mountains(src="/assets/images/mountains.svg")
            img.image-lines(src="/assets/images/lines.svg")

        form.signup-form(ng-controller="SignupController", ng-submit="submit()", ng-switch="view")

            section(ng-switch-when="signup")

                label.border-right
                    span First Name
                    input(name="firstName", ng-model="request.firstName", type="text", placeholder='Johnny', required)

                label
                    span Last Name
                    input(name="lastName", ng-model="request.lastName", type="text", placeholder='Appleseed', required)

                label.full-width
                    span Company
                    input(name="company", ng-model="request.company", type="text", placeholder='Appleseed Inc.', required)

                label.full-width
                    span Email Address
                    input(name="email", ng-model="request.email", type="email", placeholder='you@mail.com', required)

                label.full-width
                    span Phone Number (optional)
                    input(name="tel", ng-model="request.phone", type="tel", placeholder='1 (123) 456-7890')

                button.solid-btn(type="submit")
                    | Request Demo

            section.thanks(ng-switch-when="thanks")
                h1 Your request has been submitted!
                h3
                    | We will contact you shortly!<br><br>
                    | Feel free to drop us a line at<br>
                    | <a href="mailto:team@42debut.com">team@42debut.com</a>

